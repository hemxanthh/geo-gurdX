import React from 'react';

const RegistrationForm: React.FC<{ onSwitchToLogin?: () => void }> = ({ onSwitchToLogin }) => {
  // RegistrationForm component removed - no longer needed for direct dashboard access
  // To restore: recreate the component and set ENABLE_AUTH = true in App.tsx
  return null;
};

export default RegistrationForm;
